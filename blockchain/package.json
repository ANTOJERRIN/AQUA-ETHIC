{
  "name": "aqua-ethic-blockchain",
  "version": "1.0.0",
  "description": "Blockchain data integrity layer for AQUA-ETHIC water quality monitoring",
  "main": "index.js",
  "scripts": {
    "deploy": "npx hardhat run scripts/deploy.js --network localhost",
    "deploy:sepolia": "npx hardhat run scripts/deploy.js --network sepolia",
    "compile": "npx hardhat compile",
    "clean": "npx hardhat clean",
    "node": "npx hardhat node",
    "test": "npx hardhat test",
    "demo": "node terminalDemo.js help"
  },
  "keywords": ["blockchain", "ethereum", "hardhat", "water-quality", "aqua-ethic"],
  "author": "AQUA-ETHIC Team",
  "license": "MIT",
  "type": "module",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}